“适用于 无壳 无暗桩 无反调试 无复杂加密”

1.首先记录下失败登陆时的提示消息放入程序中寻找
如果没有提示，直接寻找字符串
在ollydbg中按Ctrl+G,输0040100开始，进入unicode搜索，按ctrl+F,找到关键词的那行附近，选择进入

2.找到附近跳转行【1】和call（子函数）或者是retn（输出被设在一个函数中）

3.在关键处，按F2设置断点，运行判断对否

4.修改汇编语句，改变跳转，或者变nop来暴力通过
检验运行，可执行文件--保存

                   寻找name/serial
1.寻找在输出字符串附近的跳转和call

2.可能在主函数里你的serial和正确serial对比，也可能在call里对比，习惯看堆栈窗口和信息窗口

3.读懂大致含义，name可能和serial有关，拥有简单逻辑关系

4.大致判断关键语句段后，断点单步跟踪，
如eax基本是存储子函数返回值 的寄存器，了解各种函数名

5.如果简单跟踪无法得到serial的明码，或者逻辑关系复杂，考虑写注册机






【1】跳转注释
跳转指令分三类:
 目标寄存器  源寄存器
一、无条件跳转:

JMP  ;无条件跳转

二、根据CX、ECX寄存器的值跳转:

JCXZ ;CX 为 0 则跳转
JECXZ;ECX 为 0 则跳转

三、根据EFLAGS寄存器的PSW标志位跳转, 这个太多了.
根据标志位跳转的指令:

JE   ;等于则跳转         同JZ
JNE  ;不等于则跳转        同JNZ

JA   ;无符号大于则跳转
JNA  ;无符号不大于则跳转
JAE  ;无符号大于等于则跳转        同JNB
JNAE ;无符号不大于等于则跳转   同JB

JB   ;无符号小于则跳转
JNB  ;无符号不小于则跳转
JBE  ;无符号小于等于则跳转        同JNA
JNBE ;无符号不小于等于则跳转   同JA

JG   ;有符号大于则跳转
JNG  ;有符号不大于则跳转
JGE  ;有符号大于等于则跳转        同JNL
JNGE ;有符号不大于等于则跳转   同JL

JL   ;有符号小于则跳转
JNL  ;有符号不小于则跳转
JLE  ;有符号小于等于则跳转        同JNG
JNLE ;有符号不小于等于则跳转   同JG

JZ   ;为零则跳转
JNZ  ;不为零则跳转

JS   ;为负则跳转
JNS  ;不为负则跳转

JC   ;进位则跳转
JNC  ;不进位则跳转

JO   ;溢出则跳转
JNO  ;不溢出则跳转

JP   ;为偶则跳转
JNP  ;不为偶则跳转
JPE  ;奇偶位置位则跳转      同JP
JPO  ;奇偶位复位则跳转      同JNP

OF 溢出
DF 方向（增减）
IF 中断
SF 符号
ZF 零
AF 辅助进位
PF 奇偶
CF 进位



